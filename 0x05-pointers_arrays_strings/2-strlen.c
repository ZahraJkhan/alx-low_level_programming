#include "main.h"

/**
 * _strlen - Returns length of a string
 * @s: the length of string to be determined
 *
 * Return: Length of string
 */
int _strlen(char *s)
{
	length = 0;

	while (*str++)
		length++;
	retutn (length);
}
